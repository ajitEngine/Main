pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'echo Hello, World!'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'npm test'  // Run tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                sh 'echo Deploying to production'
            }
        }
    }
}
